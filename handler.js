"use strict";

const {
  createCode,
  uploadMedia,
  setTags,
  dailyImage,
  dailyParticipation,
  dailyExchange,
  telegramAuth,
  commands,
  getCommand,
  editCommand,
} = require("./src");

module.exports = {
  createCode,
  uploadMedia,
  setTags,
  dailyImage,
  dailyParticipation,
  dailyExchange,
  telegramAuth,
  commands,
  getCommand,
  editCommand,
};
