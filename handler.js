"use strict";

const {
  createCode,
  uploadMedia,
  setTags,
  dailyImage,
  dailyParticipation,
  telegramAuth,
} = require("./src");

module.exports = {
  createCode,
  uploadMedia,
  setTags,
  dailyImage,
  dailyParticipation,
  telegramAuth,
};
